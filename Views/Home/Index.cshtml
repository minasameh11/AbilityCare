@{
    ViewData["Title"] = "الصفحة الرئيسية";
    var currentYear = DateTime.Now.Year;
}

<style>
    body {
        background: linear-gradient(135deg, #4e73df, #1cc88a);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: "Cairo", sans-serif;
        direction: rtl;
    }

    .home-container {
        background-color: #fff;
        border-radius: 20px;
        box-shadow: 0 8px 25px rgba(0,0,0,0.25);
        padding: 40px 50px;
        max-width: 750px;
        width: 100%;
        text-align: center;
        animation: fadeIn 1s ease-in-out;
    }

    .logo {
        width: 100px;
        margin-bottom: 15px;
    }

    .home-container h1 {
        font-weight: 700;
        color: #2e59d9;
        margin-bottom: 15px;
    }

    .home-container p {
        color: #555;
        margin-bottom: 35px;
        font-size: 18px;
    }

    .btn-custom {
        width: 100%;
        padding: 15px;
        border-radius: 14px;
        font-size: 18px;
        font-weight: 600;
        transition: all 0.3s ease;
    }

        .btn-custom:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 15px rgba(0,0,0,0.2);
        }

    .card-btn {
        margin: 12px 0;
    }

    footer {
        margin-top: 30px;
        font-size: 14px;
        color: #888;
    }

    @@keyframes fadeIn {
        from

    {
        opacity: 0;
        transform: translateY(25px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }

    }
</style>

<div class="home-container">
    <!-- شعار الكنيسة -->
    <img src="~/images/church-logo.png" alt="Church Logo" class="logo" onerror="this.style.display='none'" />

    <h1>✨ نظام إدارة خدمة ذوي الهمم ✨</h1>
    <p>مرحبًا بك في النظام الخاص بخدمةابناء الملكوت بجزيره شارونه  — حيث يمكنك إدارة بيانات المخدومين بسهولة وسرعة.</p>

    <div class="card-btn">
        <a asp-controller="People" asp-action="Index" class="btn btn-primary btn-custom">
            👥 إدارة الأشخاص
        </a>
    </div>

    <div class="card-btn">
        <a asp-controller="People" asp-action="Create" class="btn btn-success btn-custom">
            ➕ إضافة شخص جديد
        </a>
    </div>

    <div class="card-btn">
        <a asp-controller="People" asp-action="Dashboard" class="btn btn-info btn-custom">
            📊 لوحة التحكم
        </a>
    </div>

    <div class="text-center mt-5">
        <a asp-controller="Meetings" asp-action="Index" class="btn btn-primary btn-lg m-2">
            📅 الاجتماعات
        </a>
        <a asp-controller="People" asp-action="Index" class="btn btn-success btn-lg m-2">
            👥 الأشخاص
        </a>
    </div>


    <footer>
        © @currentYear خدمة ذوي الهمم - جميع الحقوق محفوظة ✝️
    </footer>
</div>
